#include <iostream>

using namespace std;

int main()
{

    int a,b;
    cin>>a>>b;
    if(b>a)
        a=b;
    cin>>b;
     if(b>a)
        a=b;
        cin>>b;
     if(b>a)
        a=b;
        cin>>b;
     if(b>a)
        a=b;
        cin>>b;
     if(b>a)
        a=b;
        cin>>b;
     if(b>a)
        a=b;
        cin>>b;
     if(b>a)
        a=b;
        cin>>b;
     if(b>a)
        a=b;
        cin>>b;
     if(b>a)
        a=b;
       cout<<a;
    return 0;
    /*


    int a,b,c,d,e,f,g,h,i,j,big;
    cin>>a>>b>>c>>d>>e>>f>>g>>h>>i>>j;
    big=INT_MIN;
    if(a>big)
        big=a;
         if(b>big)
        big=b;
         if(c>big)
        big=c;
         if(d>big)
        big=d;
         if(e>big)
        big=e;
         if(f>big)
        big=f;
         if(g>big)
        big=g;
         if(h>big)
        big=h;
         if(i>big)
        big=i;
         if(j>big)
        big=j;
        cout<<big;
        */

   }

